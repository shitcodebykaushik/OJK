<?xml version="1.0" encoding="utf-8"?>
<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo" class="bg-gray-100">
    <ActionBar title="Sign Up" class="bg-blue-500 text-white">
        <NavigationButton text="Back" android.systemIcon="ic_menu_back" />
    </ActionBar>
    
    <StackLayout class="p-6">
        <Label text="Create Account" class="text-2xl font-bold text-center mb-6" />
        
        <TextField hint="Full Name" 
                  text="{{ fullName }}" 
                  class="mb-4 p-4 bg-white border rounded" />
                  
        <TextField hint="Email" 
                  text="{{ email }}" 
                  keyboardType="email" 
                  autocorrect="false" 
                  autocapitalizationType="none"
                  class="mb-4 p-4 bg-white border rounded" />
                  
        <TextField hint="Password" 
                  text="{{ password }}" 
                  secure="true" 
                  class="mb-4 p-4 bg-white border rounded" />
        
        <Button text="Sign Up" 
                tap="{{ onSignUpTap }}" 
                class="bg-blue-500 text-white font-bold py-3 rounded" />
    </StackLayout>
</Page>